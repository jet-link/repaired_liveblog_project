/**
 * Admin tables - sorting, bulk actions (placeholder for future)
 */
(function() {
  'use strict';

  // Table row click - could expand to show details
  document.querySelectorAll('.admin-table tbody tr').forEach(function(row) {
    row.style.cursor = 'default';
  });

})();
